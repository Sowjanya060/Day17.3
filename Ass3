#include<stdio.h>
main()
{
	int n, i, count = 0;
    scanf("%d",&n);
    i=2;
    while( i<=n/2)
    {
        if(n%i==0)
        {
            count=1;
        }
        i++;
    }
 
    if (count==0)
        printf(" PRIME",n);
    else
        printf("NOT PRIME",n);
}
